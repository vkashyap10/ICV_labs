Directory structure : 



Results are saved wherever asked
Results are displayed where not asked to save


How to run on ITL machines

1) module load anaconda3
2) if opencv is not installed run :
	pip install --upgrade pip setuptools wheel
	pip3 install opencv-python
3) jupyter notebook to launch gui
	Each question has a separate notebook, just run all cells 
	Results are saved wherever asked in question in the following directory structure
	In all other questions results are displayed in the notebook itself.